.row
    .col-md-4.order-md-2.mb-4
        ul.list-group.mb-3
            li.list-group-item.d-flex.justify-content-between.lh-condensed
                div
                    h6.my-0 =current_user.email
                    small.text-muted =link_to('Logout', destroy_user_session_path, method: :delete)
    .col-md-8.order-md-1
        table.table.table-hover
            thead
                tr
                    th scope='col' #
                    th scope='col' Code
                    th scope='col' Name
                    th scope='col' Market Cap
                    th scope='col' Value (USD)
            tbody
                - sorted_coins.limit(10).each.with_index(1) do |coin, idx|
                    tr
                        th scope='row' =idx
                        td =coin.symbol
                        td =link_to coin.name, coin
                        td =number_with_delimiter(coin.worths.latest.first.market_capitalization)
                        td =number_with_delimiter(coin.worths.latest.first.quoted_value)